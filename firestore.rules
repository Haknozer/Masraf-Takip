rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Herkese açık - tüm koleksiyonlar okunabilir ve yazılabilir
    // ⚠️ UYARI: Bu sadece geliştirme/test için kullanılmalıdır!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

